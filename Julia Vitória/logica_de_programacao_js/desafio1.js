// Escreva um coidgo me JavaScript que resolva o problema descrito 
//no livro Lógica da Programação 1,p., em que temos temos que obter a
// nota de 3 alunos, calcular a média de 3 alunos, calcular a media e exibir se 
//o aluno foi aprovado, reprovado ou ficou de exame. não é necessario ler as notas, você pode defini-las com variaveis
// você precisará utilizar uma estrutura de decisão composta, como a seguir:
const 
      nota1 =10,
      nota2 = 6, 
      nota3 = 7;
    

 const     media = (nota1 + nota2 + nota3) / 3;

 if ( media >= 7) {
    console.log (`O aluno foi APROVADO com média ${media.toFixed(2)}
    `);
 } else if (media);

const condicao =  (nota1 + nota2 + nota3)/3;


if (condicao >= 7) {
    console.log ('Aprovado');
}
if (consicao <= 5 ){
    console.log ('Reprovado')
} else{
    console.log ('Exame')
}